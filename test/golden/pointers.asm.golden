.text
0		CALL 3
1		PUTNUM R0
2		HALT
3		PUSH BP
4		MOV BP, SP # ↑Int
5		SUB SP, 2 # ↑Int
6		MOV R1, 4 # Int
7		MOV [BP + -1], R1 # Int
8		LEA R2, [BP + -1] # ↑Int
9		MOV [BP + -2], R2 # ↑Int
10		MOV R3, [BP + -2] # Int
11		MOV R4, 8 # Int
12		MOV [R3], R4 # Int
13		MOV R5, [BP + -1] # Int
14		MOV SP, BP # ↑Int
15		POP BP # ↑Int
16		MOV R0, R5 # ◇
17		RET
