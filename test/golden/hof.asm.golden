.text
0		CALL 25
1		PUTNUM R0
2		HALT
3		PUSH BP
4		MOV BP, SP # ↑Int
5		SUB SP, 0 # ↑Int
6		MOV R1, [BP + 2] # Int
7		MOV R2, 1 # Int
8		ADD R1, R2 # Int
9		MOV SP, BP # ↑Int
10		POP BP # ↑Int
11		MOV R0, R1 # ◇
12		RET
13		PUSH BP
14		MOV BP, SP # ↑Int
15		SUB SP, 0 # ↑Int
16		MOV R3, 42 # Int
17		MOV R4, [BP + 2] # (Int) -> Int
18		PUSH R3
19		CALL R4
20		SUB SP, 1 # ↑Int
21		MOV SP, BP # ↑Int
22		POP BP # ↑Int
23		MOV R0, R0 # ◇
24		RET
25		PUSH BP
26		MOV BP, SP # ↑Int
27		SUB SP, 1 # ↑Int
28		MOV R5, 13 # ((Int) -> Int) -> Int # fun ptr
29		MOV [BP + -1], R5 # ((Int) -> Int) -> Int # fun ptr
30		MOV R6, 3 # (Int) -> Int # fun ptr
31		MOV R7, [BP + -1] # ((Int) -> Int) -> Int
32		PUSH R6
33		CALL R7
34		SUB SP, 1 # ↑Int
35		MOV SP, BP # ↑Int
36		POP BP # ↑Int
37		MOV R0, R0 # ◇
38		RET
